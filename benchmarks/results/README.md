# Recorded benchmark data

Run scripts\run_benchmarks.ps1 to regenerate machine-specific CSV results.
Generated CSV files are intentionally not committed because wall-clock values
depend on the host and installed Python runtime.
